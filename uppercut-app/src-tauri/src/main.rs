//! Placeholder. The Tauri 2 desktop app is Phase 2 work — see PLAN.md §4 and
//! docs/architecture.md. When scaffolded for real, Tauri command handlers must dispatch to
//! `uppercut_core::apply_command` exactly like `uppercut-cli` does (AGENTS.md §0.1), and the
//! video preview must be a native wgpu surface, never routed through the webview
//! (AGENTS.md §0.5).

fn main() {
    eprintln!("uppercut-app: not yet implemented (Phase 2). Use uppercut-cli for now.");
    std::process::exit(1);
}
